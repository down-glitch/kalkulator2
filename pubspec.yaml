name: calculator_app
description: A simple calculator with history and profile page.

publish_to: 'none' # karena ini aplikasi lokal

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  expressions: ^0.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


flutter_native_splash:
  color: "#ffffff"
  image: assets/splash.png
  android: true
  ios: true
 
 

